# UK001-EDIT-0014 — Paper 45 indexed-notation fidelity

## Decision

At the three Ukrainian Paper 45 loci corresponding to the activated German
ED0015 correction, change `d(y)` to `d_{i}(y)` on lines 475 and 497 and
`f(0,y)` to `f_{i}(0,y)` on line 507. Preserve every outside byte.

The overloaded uppercase `P` is retained at the five adjudicated Ukrainian
loci, and the Bemerkung-II sequence beginning with `h_{i}y^{(K)}` is retained
through the later proof variable `\lambda` and conclusion `\lambda=(K)`.

## Considerations

The first divisor belongs to the indexed pair `f_i,g_i`, is immediately used
as `d_i(y)` in both factorizations, and remains `d_i(y)` in the following
noncongruence. The second occurrence must denote that same divisor in the
construction of `G`. The coefficient in the identity derived from equation
(1) must be `f_i(0,y)`, paired with `g_i(0,y)` and the factors `f_i,g_i`.
These are probable index omissions in the 1927 print, not a license to adopt
the alternate redaction.

Keeping either bare divisor, changing only one, introducing an abbreviation,
or rewriting the argument was rejected because each would leave an undefined
or internally inconsistent index while expanding beyond the three admitted
tokens. Renaming either use of `P` was rejected as unsupported modernization.
Changing `h_i y^(K)` to introduce `\lambda` earlier was rejected because the
paragraph itself proves `\lambda=(K)`.

## Evidence and activation

The source adjudication is
`P45_FOUR_AUTHORITY_UNCERTAINTIES_ADJUDICATION_20260814.json`, 25,915 bytes,
SHA-256 `C9646D4027BE13D3F4DF77343A5D550D1C7605C25D32EEEA66010288E156D5C8`.
German ED0015 differs from ED0014 only on German lines 23917, 23926, and
23931, with exact reverse replay. `CURRENT_GERMAN_AUTHORITY_POINTER.json` is
byte-identical to pointer v052; activation receipt
`NOETH-DE-ACT-v052-r36-20260815` records CURRENT-last activation, and r36
anonymously read back all 56 files under DOI `10.5281/zenodo.21940320`.

The sealed UK001-EDIT-0013 state correctly remains historical evidence of its
ED0014/v051 context. The separately pinned activation establishes ED0015 as
the authority for this successor; history is not rewritten.

## Verification

- Input: 35,069 bytes / SHA-256 `23510C87DE041F305353DFD053427020BF9108A97B37E6CB0F5EE3E42B3116A2`.
- Output: 35,081 bytes / SHA-256 `8437DB33C1A2D42AFFCEA56EECBA74300232D81AB79C808754671E5F6299BBD9`.
- Changed lines: exactly 475, 497, and 507; raw-byte delta +12.
- Exact positional reverse restores UK001-EDIT-0013 byte-for-byte.
- All four outside segments, three context spans, 608 LF/NFC lines, labels,
  references, environments, footnotes, TeX controls, math delimiters, and
  comments passed; braces changed only by the three required `{i}` groups.
- An independent reviewer inspected all 1,167 applicator lines, independently
  reversed and replayed the source in memory, and returned ACCEPT with no
  concrete defect.
- Two pre-apply `python -B` dry runs had identical 13,137-byte stdout,
  SHA-256 `687FC9C6F32F40F6047CBB263B1A7D153948CC96C93233E615521ECC94E4E028`;
  the complete bounded working-root fingerprint remained identical.
- The source-only apply left no lock, stage, or rollback residue. All 21
  producer files, the base and other sources, frozen v047/v018, and public r35
  remain exact.

## Boundary

This is a project-authority mathematical-notation fidelity repair. It is not
native-Ukrainian validation or a critical-edition claim. It creates no build,
publication, DOI, frozen-release, or producer change.

