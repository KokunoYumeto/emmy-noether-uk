# UK001-EDIT-0015 — Paper 30 congruence-sign fidelity

## Decision

In Paper 30, section 8, item 2 (the argument referring back to section 7.3),
replace the two inherited assertions that `n` is not congruent to zero modulo
`p` with the source-supported positive congruences. The preferred local
Paper30 Section08 producer changes only lines 108 and 111. Assembled Unit122
changes those signs on lines 16902 and 16905 and changes its source and
identity receipts on lines 16804 and 16805. Every outside byte is preserved.

The immutable central producer remains adverse lineage. The working edition
derives a new local producer from it; it does not rewrite the custody source.

## Evidence and reasoning

German ED0015, 2,154,017 bytes, SHA-256
`51A25101C04877AE740989E72B2AD65A7A7E65B081077C4A518BF1737AD5B907`,
has positive `n\equiv0(\mpideal)` readings at lines 15174 and 15177. The
primary Paper 30 print, page 52, is retained in `evidence/P30/print.pdf`,
3,218,412 bytes, SHA-256
`4259EF9054DE32AE81BA1A6A66481BF951F4C33B306AA15817F841BB75FABCE2`;
the reviewed page likewise has two positive congruences. The article DOI is
`10.1007/BF01209152`.

The proof also requires the positive premise: if `b` and `n` are both in
`p`, then `bn` is in `p^2`, contradicting `mc` not in `p^2` because `bn=mc`;
hence `b` is not in `p`. The inherited negative premise cannot establish that
contradiction. The neighboring negative statements about `m`, `m^2`, `c`,
`mc`, and `b` are protected controls and remain unchanged.

Retaining either negative `n` reading was rejected as conflicting with the
primary print, active German authority, and proof. A base-only repair was
rejected because it would leave the active producer and assembled unit out of
sync. Editing the central origin was rejected because it is immutable adverse
lineage. Broad prose or notation modernization was outside scope.

## Exact replay

- Central origin: 10,357 bytes / SHA-256
  `B71723A121E19F9DBD0FC758192CB7445955330AD733BF6A2115E41050DF367A`.
- Preferred local producer: 10,349 bytes / SHA-256
  `63936A016E9FED562DFAE81BD49742AC72E3E34F021D3CA8E5ABDA850E1BB539`.
- UK001-EDIT-0014 base: 2,690,387 bytes / SHA-256
  `FC3241DF6896DBC1731DDC4AE231036288DAB9538BCAA1577A556ACC954A4FE5`.
- UK001-EDIT-0015 base: 2,690,266 bytes / SHA-256
  `16CFA035A13015DBB38D6238B844D055A44D9F0978F5E55A95D241997CACF19E`.
- Exact positional reverse restores both predecessor objects byte-for-byte.
- The projected producer body equals assembled Unit122 exactly; prefix and
  suffix remain byte-identical; only the four declared base lines change.

## Transaction review

The mathematical transform pins were exact throughout, but two transaction
implementations were rejected before apply.

1. The initial applicator, 60,975 bytes / SHA-256
   `F0A2F31B5881F7448DAC3F986C65EC383AC9AA5123605046AA4238EDD98635F8`,
   used fallible rollback probes before enabling recovery preservation and
   unlinked residue by path without invocation identity. A probe failure could
   therefore escape while cleanup removed recovery and lock, or delete a
   third-state path.
2. The first hardening, 67,544 bytes / SHA-256
   `30A9ECDEE0DE9DF050407FA0844223EE8D9B4F6494DD93323F51DB3570DAE527`,
   still registered a created path only after write, flush, fsync, and fstat.
   A fault after exclusive creation could leave untracked partial residue and
   permit a false clean rollback.

The accepted applicator is 71,909 bytes / SHA-256
`3ABE9F6309C47907275EC00B9C13452DBFA7EF5327E67860B8AA2266C87A4FA2`.
It records an attempted path before exclusive open, binds descriptor and path
device/inode/type before writing, preserves uncertain or foreign residue, and
retains lock and recovery whenever rollback is incomplete. A 24-case hostile
matrix covered write, flush, fsync, fstat, lstat, replace, verification,
rollback, unlink, cleanup, foreign path, and distinct-inode exact-content
states. It passed with run receipt
`FE478EDF770F25380D848447517DA0E63AEA941ED53632B76FF68DE8606D5F1D`.
That receipt is run-specific because captured diagnostics contain randomized
temporary roots; the inline harness was not retained as a durable artifact.

Two final no-write runs produced identical 12,275-byte stdout, SHA-256
`C1A3A0A66F605EEB0F6C813236A32485CACB5D548AA84F48CB68FF3893572738`,
and bounded manifest SHA-256
`D79F4CD5E847A2D32F9DB00F7BBD2CD430A13E664B4579E68C2575C1B70A5EA1`.
Separate final independent review reconstructed both outputs and reverses,
checked custody and authority, inspected the matrix logic for false-positive
passes, and returned ACCEPT. Apply left all four transaction residue paths
absent.

## Boundary

This is a project-authority mathematical-sign fidelity repair, not native
Ukrainian validation or a critical-edition claim. It creates no build,
publication, DOI, or frozen release. The previously closed Paper24
`NOETH-DEF-P24-0003` disposition, all Paper45 dispositions, the empty hold and
open-disposition sets, and prior publication state remain unchanged.
